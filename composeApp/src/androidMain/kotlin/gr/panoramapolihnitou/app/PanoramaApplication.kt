package gr.panoramapolihnitou.app

import android.app.Application

/**
 * Application entry point. Kept minimal; multiplatform-settings resolves the
 * application context automatically, so no manual DI initialisation is required.
 */
class PanoramaApplication : Application()
