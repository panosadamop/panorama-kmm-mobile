# Copy to `keystore.properties` (kept out of git) to enable signed release builds.
# See RUNNING.md → "Signing the Android release" for how to create the keystore.
storeFile=../release-keystore.jks
storePassword=CHANGE_ME
keyAlias=panorama
keyPassword=CHANGE_ME
